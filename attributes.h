//generated with attributes.py
#pragma once
#include "imgui_docking/imgui.h"
#include "directional_light.h" 
#include "transform.h" 
void draw(BaseObject* obj) {
if (obj->type == DIRECTIONAL_LIGHT)
{}
if (obj->type == TRANSFORM)
{}
};
